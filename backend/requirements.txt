# Kerala Police AI — Backend Dependencies
# Compatible with Python 3.13+ (use venv/Scripts/pip to install)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Database
sqlalchemy[asyncio]>=2.0.36   # 2.0.36+ fixes FastIntFlag crash on Python 3.13.3+
asyncpg>=0.30.0               # 0.30+ has cp313/cp314 prebuilt wheels
alembic>=1.14.0
psycopg2-binary>=2.9.10       # NOTE: if this fails, it will be skipped (not needed if using asyncpg)

# Auth & Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Data validation
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1

# NLP & ML (install separately if slow)
spacy>=3.8.3
scikit-learn>=1.5.2
sentence-transformers>=3.3.0

# Vector Store
chromadb>=0.5.23

# PDF Processing
pdfplumber>=0.11.0
pypdf>=4.2.0

# HTTP Client (Bhashini API calls)
httpx>=0.27.0

# Google Gemini LLM (Legal Assistant RAG)
google-genai>=1.0.0

# Background Tasks
celery>=5.4.0
redis>=5.0.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6

# Utilities
python-dateutil>=2.9.0
numpy>=2.1.0
