# Kerala Police AI — Backend Dependencies
# Python 3.13 compatible versions (updated from original pinned versions)

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.10      # 2.9.10+ has cp313 wheels

# Auth & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Data validation
pydantic==2.9.2               # pydantic-core 2.23.4 has cp313 wheels
pydantic-settings==2.5.2
python-dotenv==1.0.1

# NLP & ML
spacy==3.8.11                 # latest 3.8.x with cp313 wheels
scikit-learn==1.5.2           # 1.5.x has cp313 wheels
sentence-transformers==3.1.1  # 3.x compatible with transformers>=4.41
# torch must be installed separately — run:
# pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu

# Vector Store
chromadb==0.5.23              # 0.5.x latest; stable with Python 3.13

# PDF Processing
pdfplumber==0.11.0
pypdf==4.2.0

# HTTP Client (Bhashini API calls)
httpx==0.27.0

# Background Tasks
celery==5.4.0
redis==5.0.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6

# Utilities
python-dateutil==2.9.0
numpy==2.1.0                  # 2.x has cp313 wheels; 1.x does not
